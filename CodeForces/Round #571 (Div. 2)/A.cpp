#include <bits/stdc++.h>
using namespace std;

int main() {
    ios_base::sync_with_stdio(false);

    int a, b, c;
    cin >> a >> b >> c;
    cout << (b >= a && c >= a ? "Yes" : "No") << "\n";

    return 0;
}
